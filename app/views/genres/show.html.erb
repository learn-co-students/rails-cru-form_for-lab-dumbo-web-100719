<h2><%= @genre.name %></h2>
<% @genre.artists.map do |artist| %>
    <%= link_to artist.name, artist %>
<% end %><br>
<br><%= link_to 'Edit Genre', edit_genre_path %> | <%= link_to "Delete", genre_path(@genre), :method => :delete %> </p>

